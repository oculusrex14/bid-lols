# State

Current phase: RC5.2 MULTI-CURRENCY CORRECTNESS PATCH
Status: COMPLETE (production release; see docs/reports/RC5_2_MULTI_CURRENCY_CORRECTNESS.md)
Production: RC5.2 runtime — viewer country fixed to x-vercel-ip-country, per-currency launch floors (INR ₹1,000 / USD $50 bounties; 1,000-major parent budget; child-bounty floor), currency-neutral copy, *Rupees identifier cleanup, sample-validity invariants (app SHA f07c6e4; deployment dpl_EkzazABomEFbmTQzcys5E2YPXcLh / bidthrone-gjromvd5v; Vercel metadata gitCommitSha=f07c6e4, gitDirty NOT set; India-origin traffic verified INR on production). Previous good / ROLLBACK TARGET (newest first): dpl_BZfofYryfFtXhriUCcnjJ6RrQFLs (RC5.1, 2de3489), dpl_BmNTfoudCa2417AsevLXFcaYL7xR (RC5, c2423e5), dpl_8Rx9WTFQC3b7kuPCqPAepEuLswPd (RC4, b9a698f), dpl_7dzaSDwdvFtP6CgyGBjYi9Az5BDQ, dpl_EfRsnBWF9VJqQdJUVFmZPRUSd9wT, dpl_HaHbAwGeiojAgG3Fzpg1mxtfXd8Z, dpl_5WugdV9fwyvWthw6kBVR6DLva58i, dpl_8EUVrSncC6fPQ14237Tc1jGFoBNR, dpl_7hLTtmKTR3vPwt2o5z3CbQZctkeE, dpl_E2UdhbQVjJq3kPqKRBHaLCk8MhUQ (RC3).
Funding: OFF (no MARKETPLACE_MONEY_LIVE in any Vercel env — re-verified 2026-08-30 by `vercel env ls` + live funding chips; /test/checkout is 403-guarded). TRUST_VERIFICATION_LIVE: 0.
Objective: complete
Next: post-RC5.2 liquidity (scores/boards/market rates fill in each currency as real completed work exists; operator follow-ups: CultureBid apex DNS still broken (10.10.0.1/10.0.1.3, re-verified 2026-08-30) so www stays canonical, GSC/Bing verification, enable Vercel↔GitHub git integration, add the force-push/deletion guards on the main-branch ruleset (github.com/oculusrex14/bid-lols/rules/21853296 currently enforces CI+E2E+CodeQL on merge only — the API rule types for push guards were not accepted), remove the dead INR_PER_USD Vercel env var, enable Vercel log delivery to inspect function logs via CLI, X cache refresh via ?share=trust-v1, confirm contact@foundersbid.lol mailbox)

RC5.2 summary (2026-08-30):
- Country detection FIXED: the deployed resolver now reads `x-vercel-ip-country` (Vercel's documented client-IP country; verified against Vercel docs + Vercel's own source) instead of RC5.1's `x-vercel-sc` (the serving edge's country — wrong signal). Live verification: India-origin production traffic renders the INR context (bidthrone home `data-currency="INR"`, Founders/Culture/Bidception ₹ samples); non-IN partitions render USD.
- ONE currency policy (`CURRENCY_MONEY_POLICY` in money.ts, composed with the currency registry; server zod + engine + client forms + copy + sample tests all derive from it): bounty launch floor is per-currency — INR ₹1,000 (100,000 paise) / USD $50 (5,000 cents), enforced at boundary AND engine (no raw-minor bypass; CultureBid same rule; unknown currency 422s). Bidception parent floor = 1,000 major units in either currency (policy-sourced + engine-enforced); a child BOUNTY additionally meets the bounty floor in the parent's currency. Projects/proposals/graveyard audited: no INR-derived floors (documented in the report).
- Copy: Bidception SEO description currency-neutral ("Every amount reconciles…"); home captain example rendered from the active sample tree; captain-fee/allocation/quote/budget form labels use the record's currency; balance error formats in the parent currency; the bounties browse min-reward filter is labelled honestly (raw minor units).
- Identifiers: `feeRupees`/`allocatedRupees`/`askingPriceRupees`/`reserveRupees`/`amountRupees`/`quotedRupees` → major-unit names (API/form level only; zero DB renames; no migration).
- Invariants pinned: samples must be postable under their own currency's floors; 9 INR + 9 USD publishes nothing; a huge USD amount cannot move the INR-native score/volume; persisted records keep their currency under every viewer context; Cashfree USD rejection pre-provider-call; mobile header on all four brands at 390/360/768/1280; Most Reliable stays Bayesian.
- QA: 2047 tests (1693 mjs + 354 ts) pass; complexity 0; contrast + public-copy + og-assets + audit clean; CI 33306439547 + CodeQL 33306439556 green on f07c6e4; 77/77 critical paths + journey; 106 style-audit captures, zero overflow; production: 17/17 smoke + 124-route sweep ×4 (zero 5xx after a 6-route transient cutover window), 6 security headers, /test/checkout 403, funding chips on exactly 3 hosts, dark-first intact.
- Release: preview dpl_9VnNAauWEMEsbrxoU6uzi5DLfTBX of the exact SHA verified (metadata clean); production dpl_EkzazABomEFbmTQzcys5E2YPXcLh from a clean detached worktree (porcelain empty immediately before deploy); Vercel metadata gitCommitSha=f07c6e4, gitDirty NOT set; rollback target dpl_BZfofYryfFtXhriUCcnjJ6RrQFLs. Branch ruleset 21853296 enabled (CI+E2E+CodeQL required on merge; force-push guards still an operator item — see Next).

RC5.1 summary (2026-08-30):
- Release correctness P0: the RC5 production deployment's Vercel `gitDirty=1` root-caused and fixed — the release worktree's `node_modules` SYMLINK was untracked and non-ignored (`.gitignore` listed `node_modules/` = directories only), so Vercel recorded a dirty tree. Pattern now `node_modules`; the clean detached release-worktree protocol + "re-run porcelain immediately before deploy + verify Vercel metadata gitDirty != 1" is documented in docs/ops/DEPLOYMENT.md. RC5.1 production metadata: gitCommitSha=2de3489, gitDirty NOT set (API-verified, not local-status-verified).
- Currency law (three concepts, never merged): WORK CURRENCY persisted on every work item (bounty/project/parent at sponsor choice, child works inherit the parent, proposals quote the project currency) and always rendered as stored; VIEWER DEFAULT CURRENCY = x-vercel-sc IN→INR else→USD (deployed) / DEFAULT_VIEWER_CURRENCY dev-only, used solely for samples, new-form defaults and the Market Rates default partition; NO FX (dead Phase 00 fx.ts live-rate module deleted). Money core: explicit INR/USD registry (en-IN/en-US, 2 minor digits, ₹/$), Intl grouping, trim-zero-minor marketing display in both currencies, unknown codes fail visibly at every boundary.
- Market Rates: currency is part of the aggregate identity — marketRateFor(product, category, currency, threshold) filters `currency = $requested` in both SQL legs; /market-rates?currency=INR|USD URL-addressable (unknown values normalize to the viewer default, documented); homepage preview consumes the same gated source in the viewer currency; empty partitions stay honest (no INR numbers labelled USD).
- BI-1.0 remains INR-native and frozen: evidence outcomes carry the work currency; only INR amounts enter valueFactor (non-INR outcomes keep their factual completion evidence, economic amount scores at the documented floor 0.75 — a USD cent is never read as an INR paise); verified volume is INR-only (snapshot verified_volume_currency stays INR by contract); the fingerprint includes outcome currency; the projector persists true amount + true currency into trust_events (replacing the literal 'INR'). No model constant changed; all-INR fixtures score identically. Migration: NONE — every currency column already existed (0013/0014/0015/0016/0018).
- Payments: ProviderCapabilities.currencies (Cashfree INR-only; fake INR+USD for tests); all three funding entry points fail with unsupported_currency BEFORE any state write or provider order. New work minimum = 100,000 minor units in either currency.
- Mobile header: the standalone appearance icon is desktop-only; below md appearance exists exactly once, inside the single menu; CTA stays visible; E2E pins it (visibility-based) at 390px + desktop.
- Most Reliable: registry/methodology copy now states the pillar is a Bayesian estimate from weighted verified provider outcomes, explicitly not the literal percentage of jobs completed clean (implementation unchanged).
- QA: 2039 tests pass (1693 mjs + 346 ts, incl. new currency-core/region/sample/partition/trust-gate/provider regressions); complexity 0; contrast + public-copy + og-assets + npm audit clean; CI 33294850970 + CodeQL 33294850935 green on 2de3489; 7/7 marketplace journey (it now creates a USD bounty by viewer default on the non-deployed test runtime) + 61/61 critical paths; 106 style-audit captures, zero overflow at 390/768/1440 (incl. new /bounties/new and /market-rates?currency=USD routes) on the journey-seeded server; 17/17 production smoke + 24 spec routes + 108-route posture sweep with zero 5xx; both currency contexts verified live (production served the USD partition from this vantage; the INR set verified via the forced DEFAULT_VIEWER_CURRENCY fixture); dark-first + #0c0d10 + funding chips unchanged.
- Release: preview dpl_HEDa5SPQ83iNTwwfXq7Epy1XguhH of the exact SHA verified (gitDirty absent); production deploy from the clean detached worktree (porcelain empty immediately before deploy); funding OFF, verification OFF; rollback target dpl_BmNTfoudCa2417AsevLXFcaYL7xR.

RC5 summary (2026-08-30):
- RC4.1 trust correctness (Gate 1, shipped first): snapshot cold/warm equivalence (0019 adds trust_score_snapshots.span_days; bRaw via model-versioned roleBase over stored pillars, uncappedScore via roleScore when capped; deep-equality tests incl. capped + restricted states); fingerprint now covers restriction/reinstatement facts + model version (a cached score cannot survive a same-day restriction; reinstatement re-fingerprints and restores the exact prior score); leaderboard cache regression (warm snapshot keeps n_eff/counterparties/confidence); marginal impact null + counterfactualStatus for NR/RESTRICTED counterfactuals (never a false 0); ONE leaderboard registry (key/title/family/metric/floor/format/role/explanation) — validator, dispatch, nav, titles, formatters all derive from it; Most Reliable now ranks the BI-1.0 provider RELIABILITY PILLAR (Reliability N% · n verified outcomes, 0..1, never a 300-900 number) with divergent fixtures; homepage Market Rates preview consumes marketRateFor() + MARKET_RATE_MIN_SAMPLE (bespoke bidIndexReady SQL deleted); trust_events append-only enforced at the DATABASE level (0019 BEFORE UPDATE OR DELETE trigger, PGLite-verified in CI) in addition to the application layer. Latent P0 fixed: snapshot PK ids now hash the full user id (the 12-char prefix slice could collide across users and 500 the second write).
- RC5 design (Gate 2): two-layer law documented in 03_DESIGN_SYSTEM.md (shared operational spine + product-object layer in src/components/product-objects + styles.css PRODUCT OBJECT LAYER; presentation only, no authority); RC5 palettes (workshop paper / lilac editorial / slate console with dark graphite header / dark archival ledger) with contrast gate unchanged; Syne retired (Outfit 700 console voice); Bidthrone dark-first via DEFAULT_THEME_MODE + readMode(fallback) + modeBootScript(fallback) (SSR/first paint dark, stored preference wins, no flash, no mismatch; theme-color chrome synced); 64px sticky header (grid, ProductMark family, inset 2px accent active rule, improved network switcher, ONE mobile menu, 44px targets); funding status = moneyMode() threaded public-safely through the shell context, quiet "Funding not live" chip on the three marketplace shells.
- Product objects: FoundersBid manila work ticket (clipped tab/tape/rotated EXAMPLE ribbon; real work = same morphology, no ribbon) + job-board cards + honest empty stage + labelled Sample work + two-mode chooser; CultureBid editorial poster hero + sample brief wall (4/2/1) + real cards from stored fields only + CREATIVE/RULES/LICENSE detail grouping + deterministic local sample art (public/sample-media/culture; no Unsplash, no CSP change); Bidception allocation tree (sample reconciles exactly: 100,000 = 10,000 + 30,000 + 20,000 + 25,000 + 15,000 + 0; real workspace tree over 8/4) + role rail with three honest doors; Bidthrone sample public record (SAMPLE RECORD, @example, NR only, 0/0/0 counters, dashed review slots, neutral timeline) + ghost leaderboard (headers + ghost bars + blank rank cell) + Market Rates preview (real sample data, progress = sample completeness only) + /leaderboards board rail + single ledger + case-file public profile.
- Taxonomy enforced: Bid Index = personal 300-900 trust model; Market Rates = category pricing aggregate; separate in code, shapes, copy, and tests. Sample contract: SampleObject<T>, data-example=true + visible EXAMPLE/SAMPLE text, no sample rows in any table, no sample JSON-LD. Money: trimZeroDecimals display mode (zero paise only; accounting unchanged).
- QA: 2022 unit/integration tests pass (1693 mjs + 329 ts), complexity 0 violations, contrast + public-copy gates pass, npm audit 0, CI + CodeQL green on c2423e5, 53/53 Playwright critical paths (incl. dark-first + stored-preference + one-menu + funding posture + sample labels), 7/7 marketplace journey (fake provider), 100 style-audit captures with zero overflow at 390/768/1440 in empty AND real-data states; two CI-caught responsive fixes shipped in the release SHA (ghost-ledger min-width containment; job-card title wrap).
- Release: 0019 applied via gated preflight BEFORE the code deploy (dry-run → apply → ledger 0017/0018/0019 verified → row counts unchanged); preview of the exact SHA verified (SSO-gated, via vercel curl: dark-first, #0c0d10 chrome, sample record, no 5xx); production deploy of c2423e5 (clean detached worktree, gitDirty=0); 17/17 critical-route smoke; 55-route posture sweep (200/301/307-signin only, zero 5xx); funding OFF, verification OFF.

RC4 summary (2026-08-29):
- Bid Index redefined as the PERSONAL trust score (BI-1.0, 300–900, deterministic, versioned): eligibility 2×2 (outcomes × unrelated counterparties) else NR; value factor (log, capped), observational complexity (skills/milestones/duration/child-structure), modest recency, chronological pair damping (1–3→1.0, 4–10→0.6, 11+→0.3; self/related-party 0), Bayesian pillars per role with the fixed priors/weights, geometric-majority base, confidence (Kish n_eff × diversity × span) with shrinkage to the 0.60 prior, hard caps (major default 649→799 over 730d; 2+ majors 549→699; severe integrity RESTRICTED then 499→649) and TRUE marginal impacts (score-with minus score-without; no point tables). No black-box ML. Model-verified via 59-model/pipeline/sim tests + property invariants; NO predictive-validity claim (calibration future work).
- Trust event layer: migration 0018 (strictly additive) — trust_events (append-only, idempotent, REVERSAL-corrections), trust_score_snapshots (fingerprint-invalidated cache/audit), trust_score_appeals, trust_risk_flags (SUSPECTED never lowers a score), verification_cases/events (disabled infra), structured dispute adjudication (resolution_code/responsibility/severity_code/finalized_at + audit), review value/fairness dimensions, milestone active_at + project_milestone_extensions. Boot-gate REQUIRED_MIGRATIONS updated; gated preflight executed before the production deploy (dry-run → apply → ledger verified → row counts unchanged).
- Deterministic projector (scripts/rebuild-trust-events.mjs; production opt-in via USE_REAL_DB=1 — verified: 2 members → 0 events, empty backfill, idempotent). Blind reciprocal review reveal with ONE shared eligibility definition (public listing + scoring; 14-day window; immutability; duplicate/self-prohibition schema-enforced).
- Taxonomy: the aggregate pricing product is now Market Rates (/market-rates; MarketRateSample/marketRateFor/MARKET_RATE_MIN_SAMPLE); /bid-index rebuilt as the methodology + personal-report surface; /settings/trust private report (impacts, caps, appeals); leaderboards: most_reliable re-backed by real BI-1.0 evidence (the reliability:0 bulk bug is gone), score boards added with stricter eligibility; Bidthrone nav = Bid Index · Leaderboards · Market rates.
- P0 social cards: four deterministic 1200×630 PNG cards (scripts/generate-og-assets.mjs, sharp from SVG), versioned URLs /og/trust-v1/<product>.png via OG_ASSET_VERSION; full OG/Twitter metadata per host (verified live: distinct per-host images, zero /og.jpg references, zero legacy pay-to-rank copy); legacy og.jpg replaced with a current neutral network card. X cache note: test once with https://bidthrone.lol/?share=trust-v1 (canonical stays clean).
- Verification: 17/17 production critical-route smoke (adds /market-rates), 42/42 Playwright critical paths, 7/7 marketplace journey (fake provider), CI + CodeQL green on the release SHA and the ops commit; migration verified in the production ledger; trust backfill honest-empty and idempotent; funding OFF, verification OFF, zero 5xx observed.

RC3 summary:
- Correctness: graveyard detail status regression (controls restored via graveyardControls matrix), bounty-detail `undefined` H1 (missing `b.title` projection), graveyard includes-checkbox silent drop, bidception allocate-form missing `kind`, apply/submit feedback lost on serverFn remount — each with regression tests.
- Security: authz session failures fail visibly (auth_unavailable + log; never silent-anonymous), Cashfree consolidated into payments/provider.ts (single credential/host/signature/replay/status source; dead Phase 00 rail removed), webhook battery extended, dev-endpoint production-simulation tests pinned, /.well-known/security.txt host-aware, CSP audit documented, Dependabot + CodeQL + pinned actions + npm audit --omit=dev gate.
- Complexity: measured audit (before/after snapshots in docs/reports/) — CC max 38→15, 0 functions > 120 lines, gate enforced in CI (production code only); hotspots refactored by business responsibility.
- Network Spine: ui/ primitives (Button/Field/Money/Status/Identity/states/layout/market/data/Review), layout canvases, product accents (contrast-verified light+dark), hairline surfaces, one status/money/identity treatment.
- UX V2: FoundersBid marketplace-first (hero preview, browse rows + URL filters, 8/4 detail, 5-step creation with conditional reward fields, /post chooser); CultureBid format-first (browse cards, "What are you commissioning?" create + brief step); Bidception workspace (work-unit tree, reconciling BudgetBar, searchable eligible-captain picker, kind+spec allocation form); Bidthrone data-first (home boards, dense leaderboards with skills, Bid Index DataTable with Insufficient-sample gates); auth-aware CTAs; product-aware theme-color; network switcher.
- E2E in CI: marketplace journey (fake provider; never real rails) + 38 cross-product critical paths; visual QA harness (48 captures, 0 problems at 390/768/1440, incl. accent/border/CTA/theme-color assertions).
- Style-consistency pass (2026-08-29, post-release): dark-mode product chrome bug fixed. `data-theme` is now rendered on `<html>` at SSR (root loader) and the dark skins use compound `html[data-mode="dark"][data-theme=…]` selectors, so page/background/header/footer theme as one in light AND dark (no more bidthrone-dark bleed onto the other three skins). All raw legacy control skins (auth, profile, waitlist, create-parent, post-chooser, 404, legal, admin) collapsed onto the spine Button/ButtonLink + Field/Input/Select/CheckRow primitives (single 8px control radius, token focus, accent checkboxes); dead `--shadow-border` + the one `shadow-lg` removed (spine is shadow-free); off-canvas `max-w-*` page containers moved to the `.canvas-*` rhythm; stray 2px table dividers → hairline; unused illustration components retired. 03_DESIGN_SYSTEM.md updated to pin the spine as the single control skin.
- Light-palette temperature fix (2026-08-29): the original light skins all sat on warm beige paper, which clashed with the cool indigo/violet accents (the "blue and beige" problem). Bidthrone light is now cool porcelain (#f1f2f4) and CultureBid light is violet-leaned cool stone (#f0eff4) — the documented "cool stone" intent finally implemented; FoundersBid keeps warm paper (warm copper accent) and Bidception keeps graphite. While tuning, a new measured WCAG gate (scripts/contrast-audit.test.mjs, in CI) found and fixed pre-existing sub-AA text tiers: subtle deepened in all light skins + minimally lifted in all dark skins, up/warn deepened and danger slightly deepened in the shared light set; theme-color meta now asserts equal to the CSS page background. Dark palette structure (bg/surface/raised/fg/muted/accent) unchanged.
- Schema-drift incident + boot gate (2026-08-29, post-release): a full-surface production sweep found /bounties returning 500 on foundersbid.lol AND www.culturebid.lol (`column b.creative does not exist`). Root cause: the RC3 browse query (a369c90) selects `bounties.creative`, added by migration 0017 — which had never been applied to the production Postgres ledger (0002–0016 were in; the gated pre-deploy step had been skipped at release). Fixed by applying 0017 through the gated step (dry-run → apply → verify; strictly additive DDL) and shipping a boot gate (src/lib/schema-ledger.ts + db.server.ts): the Neon path asserts the `_migrations` ledger contains REQUIRED_MIGRATIONS before the first query — missing files fail loudly at first request with the exact names, and a CI test pins the list to the migrations/ directory. DEPLOYMENT.md step 4 is now enforced, not just documented.
- IndexNow first submission (2026-08-29): 18 public URLs from the four production sitemaps (culturebid via www) submitted with scripts/indexnow-submit.mjs --apply; all four hosts answered 202.
- Founders H1 copy fix (2026-08-29): a stray `);` text node rendered inside the hero heading (").; Get startup work done"). Removed; the homepage E2E guard is now prefix-match + no code punctuation in the H1, so a prepended artifact fails CI instead of passing the old substring check.
- Error-page sanitization (2026-08-29, P0 #2): production 500s must never leak internal error strings. AppErrorComponent now shows fixed copy ("Something went wrong" / "Try again or contact support.") in production builds (dev keeps diagnostics); the real error goes to the server log. The request-id middleware annotates every SSR 5xx document with a fixed-position "Request ID: <id>" line (injected before the hydration marker, so it survives; value === x-request-id header === log-line id) and scrubs the dehydrated `new Error("...")` payload channel. Intentional domain envelopes, 404 page, and 4xx/2xx bodies are untouched. Regression tests incl. a forced-SSR-500 end-to-end on a production build.
- Release gates (2026-08-29, P0 #4/#5): 7-point schema preflight documented in DEPLOYMENT.md step 4 (operator-side; prod DATABASE_URL never in CI); scripts/prod-critical-smoke.mjs (16 critical routes) now mandatory after every production deploy (step 10). Both executed on the current deployment: 16/16 at 200, zero 5xx, funding OFF.
- Single business inbox (2026-08-29): operator decision that contact@foundersbid.lol is the ONLY business email listed anywhere on any of the four domains. All four PRODUCTS contactEmail values, every legal doc (/terms /privacy /refund /contact), the footer, and security.txt now resolve to it; the /contact doc states there are no per-site mailboxes, and host.test.ts asserts the single-inbox invariant (no per-domain business mailbox). Verified live on all 24 surface x domain combinations.

Previous phases:
- Phase 00 (W1–W11), 00.5 (2a8edf7), 00.6 (c50cbdb): foundation, clean-SHA release, guardrails.
- Phases 01/01B/02/03/04 + RC1 (949a095): marketplace engine, capability matrix, money precision, copy pass.
- RC2 (search discovery — copy voice, blog, entity metadata, canonical origins, JSON-LD, sitemaps, IndexNow) shipped to production together with RC3's deploy.

Blocked / external (non-repo):
- culturebid.lol APEX DNS: public A records still resolve to private IPs (10.10.0.1 / 10.0.1.3, re-verified 2026-08-29) → apex unreachable; www.culturebid.lol is the canonical CultureBid origin. Fix + rollback: docs/ops/DEPLOYMENT.md ("DNS note").
- Google Search Console + Bing Webmaster Tools verification for all four properties: EXTERNAL ACTION REQUIRED (docs/ops/SEARCH_VISIBILITY.md).
- Vercel ↔ GitHub git integration + branch protection: browser-side (docs/ops/DEPLOYMENT.md).
- 4 legacy PENDING Cashfree orders: settle only via verified webhook (docs/ops/LEGACY_ORDERS.md).
- `vercel curl -X POST` beta cannot deliver request bodies (webhook POST probes return a proxy-side 500 with no function invocation) — verify the webhook through real curl against the identical local built artifact (documented in the RC3 report).